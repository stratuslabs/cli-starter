# cli-kit

A template for building command-line tools that feel finished — browser
sign-in, arrow-key menus, machine-readable output, and help that cannot drift
out of date.

The work lives on `claude/cli-template-design-hrmch0` and is under review in
the open pull request against this branch. Once it merges, this file is
replaced by the real README.
